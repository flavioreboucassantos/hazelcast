# AMBIENTE

	JDK 25
	<quarkus.platform.version>3.34.1</quarkus.platform.version>
	<com.hazelcast:hazelcast:5.6.0>
	<org.mongodb:mongodb-driver-sync:5.6.4>

# Environment Variable

	QUARKUS_PROFILE=PROFILE_NAME	
	"A profile aware file is only loaded if the unprofiled application.properties is also available in the same location and the file extension matches between the files.
	This is required to keep a consistent loading order and pair all the resources together."
	
# COMANDOS DEV

	mvnw quarkus:list-extensions
	mvnw quarkus:update
	
	# Use "-Dtest=ClassName" or Add "Test" to the Class Name.
	mvnw clean test
	
	mvnw clean compile quarkus:dev
	mvnw clean compile quarkus:dev -Dquarkus.profile=PROFILE_NAME		

# COMANDOS PROD

	- Use o formato plugin:goal@executionId
	
	Para hazelcast-server-mapstore-mongodb:
		mvn clean compile assembly:single@make-assembly -DskipTests
		java -jar target\hazelcast-server-mapstore-mongodb-0.0.1-SNAPSHOT-jar-with-dependencies.jar 
		
		CREATE JAR entity-person-etc
			mvn clean compile jar:jar@entity-person-etc
		
		INSTALL JAR entity-person-etc
			mvn install:install-file@entity-person-etc

	Para hazelcast-client-quarkus:
		"Empacota sem testes."
		mvn clean package -DskipTests
		mvnw clean package -DskipTests
		java -jar target\quarkus-app\quarkus-run.jar